"""Anchor core test suite — pure unit tests for production-shipped routing primitives.

Each module under test is exercised against the *real* implementation
(no mocks) so coverage numbers reflect what production actually runs.
This file intentionally left empty (test discovery doesn't import it).
"""